import { TEST_APPLICATION_REGISTRY } from "./application/application.registry";
import { TEST_INFRA_REGISTRY } from "./infra/infra.registry";

export const TEST_MODULE = {
  INFRA: TEST_INFRA_REGISTRY,
  APPLICATION: TEST_APPLICATION_REGISTRY,
};
